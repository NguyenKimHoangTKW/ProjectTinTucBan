@{
    ViewBag.Title = "Đăng ký tài khoản";
    Layout = null; // Sử dụng layout riêng hoặc không layout
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <!-- Bootstrap 4 CDN -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <style>
        body {
         background-image: url('@Url.Content("~/Areas/assets/images/nen.jpg")');
         background-size: cover;
         background-position: center center; /* Thêm center thứ hai để căn giữa cả chiều dọc */
         background-repeat: no-repeat;
 /* background-attachment: fixed; */ /* Bạn có thể bật lại nếu muốn nền cố định khi cuộn */
         min-height: 100vh; /* Đảm bảo body chiếm ít nhất toàn bộ chiều cao màn hình */
         margin: 0;
 /* Thêm các thuộc tính để căn giữa form nếu cần */
         display: flex;
         align-items: center;
         justify-content: center;
         padding: 15px; /* Thêm padding nhỏ cho body */
         box-sizing: border-box;
        }

        .register-form {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.10);
            padding: 32px 24px;
            width: 100%;
            max-width: 450px; /* Tăng chiều rộng một chút cho form đăng ký */
        }

            .register-form .form-control {
                border-radius: 8px;
                padding: 10px 15px;
            }

            .register-form .input-group-text {
                background: #f8f9fa;
                border-radius: 8px 0 0 8px;
            }

            .register-form h2 {
                font-weight: 600;
                margin-bottom: 25px;
            }
        .form-footer-links {
            text-align: center;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="register-form">
        <h2 class="text-center">Tạo tài khoản mới</h2>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger">@ViewBag.Error</div>
        }
        @if (ViewBag.SuccessMessage != null)
        {
            <div class="alert alert-success">@ViewBag.SuccessMessage</div>
        }

        <form method="post" action="/Admin/InterfaceAdmin/Register">
            @Html.AntiForgeryToken()

            <div class="form-group mb-3">
                <label for="username">Tên đăng nhập</label>
                <input type="text" class="form-control" id="username" name="username" placeholder="Nhập tên đăng nhập" required />
            </div>

            <div class="form-group mb-3">
                <label for="password">Mật khẩu</label>
                <input type="password" class="form-control" id="password" name="password" placeholder="Nhập mật khẩu" required />
            </div>

            <div class="form-group mb-4">
                <label for="confirmPassword">Xác nhận mật khẩu</label>
                <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" placeholder="Xác nhận lại mật khẩu" required />
            </div>
            
            @* Thêm các trường khác nếu cần, ví dụ Email, Họ tên,... *@
          
            <div class="form-group mb-3">
                <label for="email">Email</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Nhập địa chỉ email" />
            </div>
            

            <button type="submit" class="btn btn-primary btn-block">Đăng ký</button>
        </form>
        <div class="form-footer-links">
            <a href="/Admin/InterfaceAdmin/Login">Đã có tài khoản? Đăng nhập</a>
        </div>
    </div>
    <!-- Font Awesome CDN for icons (nếu cần icon) -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/js/all.min.js"></script>
</body>
</html>