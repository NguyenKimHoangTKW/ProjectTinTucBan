﻿<div>

    <style>
        .admin-topmenu-nav {
            display: flex;
            flex-direction: row;
            list-style: none;
            padding: 0;
            margin: 0;
            background: #f8f9fa;
            border-bottom: 1px solid #dee2e6;
        }

            .admin-topmenu-nav > li {
                position: relative;
                margin: 0;
            }

        .admin-topmenu-link {
            display: block;
            padding: 12px 20px;
            color: #333;
            text-decoration: none;
            font-weight: 500;
            transition: background 0.2s;
        }

            .admin-topmenu-link:hover,
            .admin-topmenu-nav > li:hover > .admin-topmenu-link {
                background: #e9ecef;
                color: #007bff;
            }

        .admin-topmenu-dropdown {
            display: none;
            position: absolute;
            left: 0;
            top: 100%;
            min-width: 180px;
            background: #fff;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            z-index: 1000;
            list-style: none;
            padding: 0;
            margin: 0;
            border: 1px solid #dee2e6;
        }

        .admin-topmenu-nav > li:hover > .admin-topmenu-dropdown {
            display: block;
        }

        .admin-topmenu-dropdown li {
            width: 100%;
        }

        .admin-topmenu-dropdown a {
            display: block;
            padding: 10px 18px;
            color: #333;
            text-decoration: none;
            white-space: nowrap;
            transition: background 0.2s;
        }

            .admin-topmenu-dropdown a:hover {
                background: #f1f3f5;
                color: #007bff;
            }

        .test-container {
            height: fit-content;
            width: stretch;
            background: #ff0000;
        }
    </style>
    <ul class="admin-topmenu-nav"></ul>
</div>
<script src="~/Scripts/jquery-3.7.0.js"></script>
<script src="~/Areas/Admin/Scripts/TopNavShowScript.js"></script>